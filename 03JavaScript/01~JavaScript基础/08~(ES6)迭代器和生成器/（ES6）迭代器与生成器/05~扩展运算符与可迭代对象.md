## 扩展运算符与可迭代对象
```js
// 扩展运算符可以使用在任意的迭代对象，是将迭代对象转换为数组的最简单的方法
let set = new Set([1, 2, 3, 4, 1, 2, 4]);
[...set] // [1, 2, 3, 4]

let map = new Map([['name', 'liuyaohui'], ['age', 24]])
[...map] // [['name', 'liuyaohui'], ['age', 24]]
```