## 解构赋值给同名变量
```js
let person = {
  name: 'liu',
  age: 24
};
let name = 'liuyaohui';
let age = 25;
// 存在则覆盖原值，不存在则创建新的变量
/*
 注意，因为{}会将其认为是代码块语句，而代码块语句不能出现在=号左侧
 整个表达式使用圆括号包括，表明应该被解析为表达式，从而可以完成赋值操作
*/
({ name, age } = person);
console.log(name, age); // liu 24
```