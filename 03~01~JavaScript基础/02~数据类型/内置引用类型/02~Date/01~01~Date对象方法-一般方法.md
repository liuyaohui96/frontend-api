
## 一般方法
```js
// 接收表示日期的字符串参数，根据这个字符串返回相应日期的毫秒数
// 不能表示日期，那么它会返回 NaN
Date.parse() 

/*
Date.UTC()的参数分别是
年份、基于 0 的月份（一月是 0，...）、月中的哪一天（1 到 31）、小时数（0 到 23）、分钟、秒以及毫秒数
*/
Date.UTC()

// 返回表示调用这个方法的日期和时间的毫秒数
Date.now()
```