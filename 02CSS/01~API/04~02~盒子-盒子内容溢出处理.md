
## 内容溢出的处理属性
```css
/* === overflow 定义如何处理盒子的内容溢出 */
/* 
scroll ，必会出现滚动条。
auto ，子元素内容大于父元素时出现滚动条。
visible ，溢出的内容出现在父元素之外。
hidden ，溢出隐藏。
 */
overflow: visible;
overflow: hidden;
overflow: scroll;
overflow: auto;
```