
## jQuery 元素内容
```js
$(selector).html(); // 包括元素标签
$(selector).text(); // 返回文本内容

// === 可以使用回调函数的形式
// index 是 元素在元素列表中的下标，old是原来元素的内容，返回值作为新的内容
$(selector).html((index, old) => {
  return old + index;
})
// text 同理，不再列举
```