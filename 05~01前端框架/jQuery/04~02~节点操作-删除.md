
## JQuery 删除节点
```js
/*
区分用法：
1. remove() 删除选中元素，包括所有文本及其子元素，该方法也会移除删除元素的data&event
2. empty 删除元素内所有的内容
3. detach 删除元素，但保持data & event
*/
// 删除内部指定选择器内容，
$(selector).remove([selector])
$(selector).empty()
$(selector).detach()

```