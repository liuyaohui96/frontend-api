
## 历史状态的管理
HTML5 通过histroy对象对历史状态进行管理

```js
/*
=== history.pushState(stateObj, title, url)
执行了概念股方法后，新的状态信息就会被加入到历史状态栈中，
浏览器并不会向服务器发送请求

pushState之后，可以使用回退按钮，按下回退按钮，就会触发popstate事件
*/
foo.addEventListener('popstate', function(event){
    // event.state 包含了当时pushsate使用的状态参数对象
})


/*
=== replaceState(stateObj, title) 
调用该方法不会再历史状态栈中创建新的状态，只会重写当前状态
*/
```