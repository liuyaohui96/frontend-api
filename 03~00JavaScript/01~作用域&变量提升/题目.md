
## 输出
```js
(() => {
  let x, y
  try {
    throw new Error()
  } catch (x) {
    (x = 1), (y = 2) // y 在全局上做了声明
    console.log(x) // 1 x是形参
  }
  console.log(x) // undefined
  console.log(y) // 2
})()
```