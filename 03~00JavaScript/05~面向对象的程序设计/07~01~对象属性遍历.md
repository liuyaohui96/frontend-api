## 对象属性遍历
```js
Object.keys(obj) // 返回一个数组，包含可枚举的属性

Objjecgt.getOwnPropertyNames(obj) // 返回一个数组，包含所有属性

/*
扩展 ------->
检查给定的属性名是否直接存在于对象中（而不是在原型链上）且为可枚举属性
obj.propertyIsEnumerable(property) 
*/

// for...in 遍历可枚举属性
for(let key in obj){
  // ...
}


```


