
## 系统对话框
```js
// === alert 警告对话框
alert(str)

// === confirm 确认对话框
confirm(str) // 出现ok和cancel两个按钮
// ok返回 true
// cancel 返回 false

// === prompt 提示用户输入
prompt(questionStr[,defaultStr])
// 点击ok按钮 返回输入框中的文本值
// 点击cancel 返回null
```